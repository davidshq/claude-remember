{
  "name": "claude-session-logger",
  "version": "0.3.3",
  "description": "Claude Code plugin that logs sessions to markdown files and SQLite database",
  "type": "module",
  "main": "src/handler.ts",
  "scripts": {
    "handler": "bun run src/handler.ts",
    "test": "bun test",
    "install-hooks": "bun run scripts/install.ts",
    "uninstall-hooks": "bun run scripts/uninstall.ts",
    "version": "bun run scripts/bump-version.ts",
    "version:minor": "bun run scripts/bump-version.ts minor",
    "version:major": "bun run scripts/bump-version.ts major"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "author": "",
  "license": "MIT"
}
